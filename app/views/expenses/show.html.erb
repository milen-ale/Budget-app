<p style="color: green"><%= notice %></p>

<%= render @expense %>

<div>
  <%= link_to "Edit this expense", edit_budget_category_expense_path(@expense)%> |
  <%= link_to "Back to expenses", budget_category_expenses_path %>

  <%= button_to "Delete", budget_category_expense_path, method: :delete %>
</div>
