<%= form_with model: budget_category, url: budget_categories_path do |form| %>
  <% if budget_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(budget_category.errors.count, "error") %> prohibited this budget_category from being saved:</h2>

      <ul>
        <% budget_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :icon, style: "display: block" %>
    <%= form.text_field :icon, placeholder: "Use image url link" %>
  </div>

  <div>
    <%=form.submit "Submit", class: "devise--2",  disable_with: "Submitting..." %>
  </div>
<% end %>
